package code.entities;

import code.Maze;
import code.utilities.Path;

public class Ghost extends Entity{
    public Ghost(int x, int y) {
        super(x, y);
    }

    public Path chase() {
//        do {
//            dir = (int)(Math.random() * 4);
//        }while(Maze.spaceValid(x+dirX[dir],y+dirY[dir]);

//        map[y][x] = prev;
//        x += dirX[dir];
//        y += dirY[dir];
//        prev = map[y][x];
//        map[y][x] = mapStates.GHOST;

        return null;
    }
}
